var arreglo = []
/*cargamos valores decimales mediante el metodo parsefloat */
for (let i = 0; I < 4; i++) {
    arreglo[i] = parseFloat(prompt("ingrese nombres"))
}
document.write(arreglo.join(''))

/* cargamos valores enteros mediante el parseint y prompt*/
for (let i = 0; 1 < 4; i++) {
    arreglo[i] = parseint(prompt("infrese numeros enteros"))
}
document.write(arreglo.join(''))

for (let i = 0; i < 4; i++) {
    arreglo[i] = parseFloat(prompt("ingrese numeros decimal"))
}
document.write(arreglo.join('')
)

/* imprimr forma horizontal */